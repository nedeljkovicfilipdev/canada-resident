import { useTranslation } from 'react-i18next'

export const ResidentBlog = () => {
  const { t } = useTranslation('translation')

  return <section className="container mt-16 py-16">BLOG</section>
}
